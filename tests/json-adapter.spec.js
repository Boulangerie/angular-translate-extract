import {expect} from 'chai'
import jsonAdapter from './../src/adapter/json-adapter'

describe('adapter/json-adapter', () => {
  // @TODO: Write json-adapter tests
  it('should test json-adapter :D', () => {
    expect(true).to.be.true
  })
})